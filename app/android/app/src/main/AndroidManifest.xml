<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The one permission. The taps under the voice have to be felt on every
         phone, and the screen's own haptics go silent wherever touch
         feedback is switched off in the system settings. VIBRATE is granted
         at install without asking, and reaches nothing but the motor. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:label="KotoLang"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Accept text shared from another app. This is how a long AI
                 reply reaches KotoLang without being copied by hand: it is
                 shared from the assistant's own app instead. Needs no
                 permission, and gives KotoLang nothing the user did not
                 deliberately send. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets url_launcher see that a browser or an assistant's own app can
             handle the links on the setup screens. Visibility only: it grants
             no ability to read anything from those apps. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <!-- The assistants' own schemes. Needed so the setup screens can tell
             whether an app is installed and open it directly: an https link
             only reaches the app while "open supported links" is enabled for
             it, which is off by default on many phones. Visibility only. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="chatgpt"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="claude"/>
        </intent>
    </queries>
</manifest>
