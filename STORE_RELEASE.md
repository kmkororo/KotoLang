# ストア公開チェックリスト

Google Play / App Store の審査で止まりやすい点と、KotoLang での対応状況。
コードで対応できるものは実装済み、アカウント作業が要るものは手順を書いています。

---

## アプリ側で対応済み

| 項目 | 状態 |
|---|---|
| プライバシーポリシー本文 | `PRIVACY.md`（日英）+ アプリ内「設定 → プライバシーポリシー」 |
| Android の権限 | **ゼロ**。リリースAPKに `uses-permission` は自動生成の内部用のみ（`aapt2 dump permissions` で確認済み） |
| targetSdk | 36（Play の最低要件を満たす） |
| iOS プライバシーマニフェスト | `ios/Runner/PrivacyInfo.xcprivacy`。収集データなし、トラッキングなし、必須理由API 3件を申告 |
| 輸出コンプライアンス | `ITSAppUsesNonExemptEncryption = false` を Info.plist に記載。毎回の質問が不要になる |
| データ削除手段 | アプリ内で全削除可能（設定 → 削除とやり直し → すべて消して最初からやり直す） |
| 署名鍵の漏洩防止 | `key.properties` / `*.jks` / `*.keystore` は `.gitignore` 済み |

---

## 公開前に必ず必要な作業

### 1. リリース署名鍵をつくる（Play の必須要件）

**現状はデバッグ鍵で署名されており、このままでは Play にアップロードできません。**

```bash
keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

作った `.jks` は Git に入れず、安全な場所に保管してください。次に
`app/android/key.properties` を作ります（このファイルも Git 管理外です）。

```properties
storePassword=<設定したパスワード>
keyPassword=<設定したパスワード>
keyAlias=upload
storeFile=<upload-keystore.jks への絶対パス>
```

`android/app/build.gradle.kts` は `key.properties` があればそれを使い、無ければ
デバッグ鍵に落ちる作りになっているので、置くだけで切り替わります。

> **鍵を失うとアプリを更新できなくなります。** Play App Signing に登録し、
> upload key を紛失した場合の再発行手段を確保しておくことを勧めます。

### 2. プライバシーポリシーを公開して URL を登録

`PRIVACY.md` を GitHub Pages などで公開し、その URL を

- Play Console → アプリのコンテンツ → プライバシーポリシー
- App Store Connect → App のプライバシー → プライバシーポリシー URL

に登録します。

### 3. Play の「データセーフティ」フォーム

すべて「いいえ / 収集しない」で回答します。

- データを収集または共有していますか → **いいえ**
- 保存データの暗号化 → 端末外に送信しないため該当なし（設問が出た場合は「データを収集していない」を選択）
- ユーザーがデータ削除をリクエストできますか → **はい**（アプリ内で全削除可能）

### 4. App Store の「App のプライバシー」

「データを収集していません（Data Not Collected）」を選択します。

---

## 審査で聞かれやすい点と答え

**Q. 外部の AI サービスを使うアプリでは？**
アプリは AI と通信しません。プロンプトをクリップボードにコピーし、利用者の操作で
AI のアプリ／サイトを開くだけです。何を入力するかは利用者が決め、その内容は各 AI
サービスのポリシー下にあります。この点はプライバシーポリシーとアプリ内に明記済みです。

**Q. ユーザー生成コンテンツの管理（UGC ガイドライン）は？**
取り込んだ教材は端末内にのみ保存され、他の利用者へ配信・共有される仕組みがありません。
公開の場がないため UGC のモデレーション要件には該当しません。

**Q. 最低限の機能しかない（Minimum Functionality）と判断されない？**
7 形式の問題、SRS による復習スケジュール、進捗の可視化を備えており、単なる
ウェブサイトのラッパーではありません。スクリーンショットに学習画面と進捗画面を
必ず含めてください。

**Q. 子ども向けですか？**
いいえ。対象年齢は一般（Everyone）とし、子ども向けカテゴリでは申請しません。

---

## 未対応・要検討

- **アプリ内のバックアップ復元導線** — 設定からは復元できますが、初回起動時
  （データが無い状態）には復元の入口がありません。機種変更直後の利用者が
  詰まる可能性があります。
- **`android:allowBackup`** — 既定の `true` のままです。Google の自動バックアップに
  学習データが含まれます。端末内のみという説明と厳密に揃えるなら `false` に
  する選択肢があります（その場合、機種変更時は手動バックアップのみになります）。
- **スクリーンショット・ストア掲載文** — 未作成です。
